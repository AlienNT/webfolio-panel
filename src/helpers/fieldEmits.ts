export const fieldEmits = {
  onFocus: (value: string): string => value,
  onBlur: (value: string): string => value,
  onInput: (value: string): string => value,
  onChange: (value: string): string => value
}